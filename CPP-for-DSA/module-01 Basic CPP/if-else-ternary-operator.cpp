#include <iostream>

using namespace std;

int main()
{
    // * in c and c++ if-else condition 100% same

    // * ternary operator is the shorter version of if-else condition

    int x = 100;
    int y = 20;

    (x > y) ? cout << "X Is Greater then Y" : cout << "Y is Greater then X";

    return 0;
}
